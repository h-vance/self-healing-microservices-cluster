apiVersion: v1
kind: Service
metadata:
  name: mongo-express-service
spec:
  selector:
    app: mongo-express
  type: {{ .Values.mongoExpress.service.type }}
  ports:
    - protocol: TCP
      port: 8081
      targetPort: http
